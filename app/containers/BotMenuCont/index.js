import React from 'react';
import { BotMenu } from '../../components';

const styles = {
 
};

/* eslint-disable react/prefer-stateless-function */
export default class BotMenuCont extends React.PureComponent {
  render() {
    return (
      <BotMenu />
    );
  }
}
